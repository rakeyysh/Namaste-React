{
  "name": "namaste-react",
  "version": "1.0.0",
  "description": "This is Namaste React by Akshay Saini",
  "main": "App.js",
  "scripts": {
    "test": "jest"
  },
  "keywords": [
    "react",
    "namaste",
    "react",
    "rakesh",
    "mishra"
  ],
  "author": "Rakesh Mishra",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.15.0"


    // ^ sign used to upgrade the parcel version like from 2.15.0 -> 2.15.1 (minior upgrade)
    // similarly ~ sign used to upgrade parcel version from 2.15.0 -> 3.0.0 (major upgrade)
    // always use ^  
  
  }

  // The file package-lock.json keeps the track of every exact version of package/dependencies which installed.

  // node_modules : is basically data of our dependencies which we have installed.
}
  